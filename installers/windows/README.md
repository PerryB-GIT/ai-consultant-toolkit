# Legacy NSIS Installer (Deprecated)

This directory contains a standalone NSIS-based Windows installer from an earlier version of the toolkit.

**This installer is NOT used by the CI pipeline and is NOT distributed to users.**

The canonical Windows installer is built from `../electron/` using electron-builder.

This directory is kept for reference only. Do not update it.
